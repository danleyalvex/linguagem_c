// Escreva um programa que declare 6 variáveis do tipo caractere e atribua a elas as letras a, l, u, n, o, s. O programa deverá imprimir, usando todas as variáveis declaradas, o seguinte resultado:
//     a + l + u + n + o + s.

#include <stdio.h>

int main () {
     char a = 'a';
     char l = 'l';
     char u = 'u';
     char n = 'n';
     char o = 'o';
     char s = 's';

     printf("O resulto das strings %c %c %c %c %c %c", a,l,u,n,o,s);
}

